# Restauran-catalogue3
The website implements test-driven development, incorporates testing for applications, utilizes end-to-end (e2e) testing, compresses images using imagemin, implements image responsiveness, uses webp as the image format, applies lazy load image techniques, and breaks bundles to maintain small file sizes.
